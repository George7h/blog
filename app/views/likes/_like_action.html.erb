<%= form_with model: like, url: user_post_likes_path(user, post), method: :post do |form| %>
  <div>
    <%= form.hidden_field :post_id, value: post.id %>
    <%= form.hidden_field :user_id, value: user.id %>
    <%= form.submit "Liked the post" %>
  </div>
<% end %>
